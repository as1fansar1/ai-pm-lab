# Next Steps

1. Add first `/analyze` endpoint with deterministic mock + response schema
2. Wire dependency installation + smoke script (`make smoke` or equivalent) to run health test quickly
3. Add OpenRouter response parser utility to extract assistant text safely from completion payload
